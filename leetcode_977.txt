class Solution {
    public int[] sortedSquares(int[] nums) {
        
       
        for(int i=0;i<=nums.length-1;i++){
         nums[i]=nums[i]*nums[i];//replacing with the squares of the elements
        }
        Arrays.sort(nums);//sorting the replaced elements
        return nums;
    }
}