class Solution {
    public int largestAltitude(int[] gain) {
        int max=0,a=0;

        for (int i:gain){
            a+=i;   //every element 
            if(a>max)
            max=a;

        }
        return max;
    }
  
}